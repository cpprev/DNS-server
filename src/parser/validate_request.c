#include "parser/validate_request.h"

RCODE validate_request(string *req_bits)
{
    // TODO go around each bit like when we parse the request but here we validate it.
    (void) req_bits;
    return NO_ERR;
}
